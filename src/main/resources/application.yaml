server:
  port: 8080

spring:
  application:
    name: gateway

  cloud:
    gateway:
      routes:
        - id: introducao-api
          uri: http://localhost:8081
          predicates:
            - Path=/**

logging:
  level:
    org.springframework.cloud.gateway: DEBUG